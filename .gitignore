# Node
node_modules/
npm-debug.log*

# .NET
bin/
obj/
*.user

# Build output
publish/
installer/dist/

# Local config / secrets — never commit host passwords or TURN credentials
.env
*.local.json
docker/turnserver.local.conf
# Server runtime state: persistent host token → ID map (contains live host tokens)
idmap.json
# Server runtime state: admin groups + client metadata
admin.json
# Server runtime state: accounts/orgs/groups DB + session secret
server/data/
data/
# Update artifacts served by the Pi (published exes + manifest)
server/update/
update/

# OS
Thumbs.db
desktop.ini
spike/

# Claude Code local state + worktrees
.claude/
