<Window x:Class="RemoteDesktop.Master.RemoteBrowserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Transfer files" Height="580" Width="1000"
        WindowStartupLocation="CenterOwner" WindowStyle="None" AllowsTransparency="True"
        Background="Transparent" ResizeMode="NoResize" FontFamily="Segoe UI">
    <Window.Resources>
        <SolidColorBrush x:Key="Line" Color="#232538"/>
        <SolidColorBrush x:Key="Card" Color="#12141F"/>
        <SolidColorBrush x:Key="Muted" Color="#8A8DA3"/>
        <LinearGradientBrush x:Key="Grad" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#5B5BF5" Offset="0"/><GradientStop Color="#9D5CFF" Offset="0.55"/><GradientStop Color="#3EC8FF" Offset="1"/>
        </LinearGradientBrush>

        <Style x:Key="PaneBtn" TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource Muted}"/>
            <Setter Property="Background" Value="#1A1C2B"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="Margin" Value="0,0,6,0"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="b" Background="{TemplateBinding Background}" CornerRadius="7"
                                BorderBrush="{StaticResource Line}" BorderThickness="1" Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="b" Property="BorderBrush" Value="#5B5BF5"/></Trigger>
                            <Trigger Property="IsEnabled" Value="False"><Setter Property="Opacity" Value="0.45"/></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DirBtn" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="b" CornerRadius="999" Padding="18,0">
                            <Border.Background><StaticResource ResourceKey="Grad"/></Border.Background>
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="b" Property="Opacity" Value="0.92"/></Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="b" Property="Background" Value="#1A1C2B"/>
                                <Setter Property="Foreground" Value="{StaticResource Muted}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- dark file list -->
        <Style x:Key="FileList" TargetType="ListView">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#E6EDF3"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
        </Style>
        <Style x:Key="FileItem" TargetType="ListViewItem">
            <Setter Property="Foreground" Value="#E6EDF3"/>
            <Setter Property="Padding" Value="6,7"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Border x:Name="b" Background="Transparent" CornerRadius="7" Padding="{TemplateBinding Padding}">
                            <GridViewRowPresenter Content="{TemplateBinding Content}" Columns="{TemplateBinding GridView.ColumnCollection}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="b" Property="Background" Value="#1A1C2B"/></Trigger>
                            <Trigger Property="IsSelected" Value="True"><Setter TargetName="b" Property="Background" Value="#26284A"/></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="#0B0C15" CornerRadius="18" BorderBrush="{StaticResource Line}" BorderThickness="1">
        <Grid Margin="26,22,26,24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- header -->
            <Grid Grid.Row="0" x:Name="Header" Margin="0,0,0,18" Background="Transparent">
                <StackPanel>
                    <TextBlock Text="Transfer files" Foreground="#F3F4F9" FontSize="19" FontWeight="Bold"/>
                    <TextBlock x:Name="Subtitle" Foreground="{StaticResource Muted}" FontSize="12.5" Margin="0,3,0,0"/>
                </StackPanel>
                <Button x:Name="CloseBtn" Content="✕" Click="CloseBtn_Click" HorizontalAlignment="Right" VerticalAlignment="Top"
                        Width="34" Height="34" Foreground="#E6EDF3" Cursor="Hand" FontSize="14">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="b" Background="#1A1C2B" CornerRadius="17"><ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/></Border>
                            <ControlTemplate.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter TargetName="b" Property="Background" Value="#26283A"/></Trigger></ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>

            <!-- panes -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- LOCAL pane -->
                <Border Grid.Column="0" Background="{StaticResource Card}" CornerRadius="12" BorderBrush="{StaticResource Line}" BorderThickness="1" Padding="14">
                    <DockPanel>
                        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
                            <TextBlock Text="🖥" FontSize="13" Margin="0,0,7,0" VerticalAlignment="Center"/>
                            <TextBlock Text="This computer" Foreground="#F3F4F9" FontWeight="Bold" FontSize="13.5" VerticalAlignment="Center"/>
                        </StackPanel>
                        <Grid DockPanel.Dock="Top" Margin="0,0,0,8">
                            <Border Background="#0B0C15" CornerRadius="7" Padding="10,6" Margin="0,0,74,0">
                                <TextBlock x:Name="LocalPath" Text="This PC" Foreground="{StaticResource Muted}" FontFamily="Consolas" FontSize="12" TextTrimming="CharacterEllipsis"/>
                            </Border>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <Button x:Name="LUp" Content="↑" Style="{StaticResource PaneBtn}" Width="30" Click="LUp_Click" ToolTip="Up"/>
                                <Button x:Name="LRefresh" Content="⟳" Style="{StaticResource PaneBtn}" Width="30" Margin="0" Click="LRefresh_Click" ToolTip="Refresh"/>
                            </StackPanel>
                        </Grid>
                        <ListView x:Name="LocalList" Style="{StaticResource FileList}" ItemContainerStyle="{StaticResource FileItem}"
                                  MouseDoubleClick="LocalList_DoubleClick" SelectionChanged="Sel_Changed" SelectionMode="Single">
                            <ListView.View>
                                <GridView><GridViewColumn Header="Name" Width="300" DisplayMemberBinding="{Binding Name}"/>
                                          <GridViewColumn Header="Size" Width="90" DisplayMemberBinding="{Binding SizeText}"/></GridView>
                            </ListView.View>
                        </ListView>
                    </DockPanel>
                </Border>

                <!-- center direction column -->
                <StackPanel Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <Button x:Name="SendBtn" Content="→ Send" Style="{StaticResource DirBtn}" Width="110" IsEnabled="False" Click="Send_Click"/>
                    <Button x:Name="GetBtn" Content="← Get" Style="{StaticResource DirBtn}" Width="110" Margin="0,12,0,0" IsEnabled="False" Click="Get_Click"/>
                    <TextBlock x:Name="DirHint" Text="Select a file, then choose direction" Foreground="{StaticResource Muted}"
                               FontSize="11" TextAlignment="Center" TextWrapping="Wrap" Width="120" Margin="0,14,0,0"/>
                </StackPanel>

                <!-- REMOTE pane -->
                <Border Grid.Column="2" Background="{StaticResource Card}" CornerRadius="12" BorderBrush="{StaticResource Line}" BorderThickness="1" Padding="14">
                    <DockPanel>
                        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
                            <TextBlock Text="🌐" FontSize="12" Margin="0,0,7,0" VerticalAlignment="Center"/>
                            <TextBlock x:Name="RemoteName" Text="Remote" Foreground="#F3F4F9" FontWeight="Bold" FontSize="13.5" VerticalAlignment="Center"/>
                            <TextBlock x:Name="RemoteId" Text="" Foreground="{StaticResource Muted}" FontFamily="Consolas" FontSize="11.5" Margin="8,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                        <Grid DockPanel.Dock="Top" Margin="0,0,0,8">
                            <Border Background="#0B0C15" CornerRadius="7" Padding="10,6" Margin="0,0,74,0">
                                <TextBlock x:Name="RemotePath" Text="This PC" Foreground="{StaticResource Muted}" FontFamily="Consolas" FontSize="12" TextTrimming="CharacterEllipsis"/>
                            </Border>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <Button x:Name="RUp" Content="↑" Style="{StaticResource PaneBtn}" Width="30" Click="RUp_Click" ToolTip="Up"/>
                                <Button x:Name="RRefresh" Content="⟳" Style="{StaticResource PaneBtn}" Width="30" Margin="0" Click="RRefresh_Click" ToolTip="Refresh"/>
                            </StackPanel>
                        </Grid>
                        <ListView x:Name="RemoteList" Style="{StaticResource FileList}" ItemContainerStyle="{StaticResource FileItem}"
                                  MouseDoubleClick="RemoteList_DoubleClick" SelectionChanged="Sel_Changed" SelectionMode="Single">
                            <ListView.View>
                                <GridView><GridViewColumn Header="Name" Width="300" DisplayMemberBinding="{Binding Name}"/>
                                          <GridViewColumn Header="Size" Width="90" DisplayMemberBinding="{Binding SizeText}"/></GridView>
                            </ListView.View>
                        </ListView>
                    </DockPanel>
                </Border>
            </Grid>

            <TextBlock x:Name="StatusText" Grid.Row="2" Text="Connecting…" Foreground="{StaticResource Muted}"
                       FontSize="12" Margin="2,12,0,0" TextTrimming="CharacterEllipsis"/>
        </Grid>
    </Border>
</Window>
